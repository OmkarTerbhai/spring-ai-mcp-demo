spring:
  application:
    name: mcp


    spring:
      ai:
        mcp:
          server:
            name:todo-management-server
            version:1.0.0
            type:SYNC
    # spring.ai.mcp.server.sse-message-endpoint=/mcp/messages

    server:
      port:8081
